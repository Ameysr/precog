# SynthGen — Universal Synthetic Conversation Generator

Generate realistic, frustration-embedded synthetic conversations for ANY company, feed into Agnost for pre-production agent fixing.

## Usage

```bash
pip install -r requirements.txt
playwright install
cp .env.example .env  # add your OPENAI_API_KEY
python run.py
```

Enter any company URL. Output saved to `output/`.
